
***************************************************************
Deserialization can be acheived through 2 ways:
ObjectMapper library - we have implemenetd that thouh JsonUtil class
JsonUtils.java
*************************************************************
JayWay Library - this is prefered way as it handles big JSON files with ease. Creating POJO classes for big json files is high maintenance. 
this library provide a .parse method which taken json response as string as a parameter. It returns a ReaContext type reference. We can pass jsonquery to .read method and it returns
the desired output.
JsonPathValidatorUtil.java is implemented in our frameowrk for this.
ProductAPITestJayWayJsonPath.java is the test.
************************************************************
command to generate report: allure serve allure-results, 
Below is the code added in base test for allure to generate reports.
	@BeforeSuite
	public void setupAllureReport() {
		
		RestAssured.filters(new AllureRestAssured());
	}
*************************************************************
Data Driven @DataProvider - returns 2 dimension Object array.
************************************************************
git init                            # create local repo
git remote add origin https://github.com/san1982-del/RestAssuredAPIFramework.git  # link GitHub
git status						    # check changes					
git add .                           # stage files
git commit -m "reason"				# save snapshot
git branch -M main					# rename branch
git push -u origin main				# push to GitHub
###############################
origin = default name for the remote
-m = commit message
-M = force rename (even if branch exists)
origin → remote repo
main → branch name
-u → sets upstream tracking
Commit is local, push is remote
After making changes repeat add, commit, push commands
###############################
Jenkins;
Tools;
1. Specify JDK path
2. Speciy Maven name, and slect install automatically
3. allure commandline installation
save
Plugins:
1. Maven integration
2. testNG results
3. HTML publisher: chaintest -- Index.html
4. allure: alllure report

New Item- Maven Project-git-braanch(main)-Build(goals and options: clean install)-Post-build Actions(Publish TestNG Results)
Publish HTML reports(

clean install - this means compile my project and run the test cases




